# DiPertin — Versão 1.2.1 (versionCode 15)

Data: 27/04/2026  
Arquivo: `build/app/outputs/bundle/release/app-release.aab`

---

## Notas para a Play Console (pt-BR, ≤500 caracteres)

```
Correção da versão 1.2.1:
• Sessão de 24h: após expirar e fazer login de novo, o app deixava de reconhecer o acesso e pedia sair de novo. Corrigido — o login volta a entrar normalmente.
• Demais melhorias de estabilidade.
```

---

## Detalhe (interno)

- **Sessão local 24h:** `ms == null` após `limparSessao` + corrida com `authStateChanges` fazia `sessaoExpirada` tratar como expirado antes de `registrarLoginAgora`. Ajuste: `garantirTimestampSessaoSeAusente` no AppGuard + `null` não significa expirado na comparação.

| Campo | Valor |
|---|---|
| versionName | 1.2.1 |
| versionCode | 15 |
| applicationId | com.dipertin.app |
